spring:
  application:
    name: item-service
  cloud:
    config:
      override-none: true
  #配置端口号  
server:
  port: 8001

eureka:
  client:
    service-url:
      defaultZone: http://eureka1:2001/eureka, http://eureka2:2002/eureka
